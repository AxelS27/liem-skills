# ⚡ Quick Benchmark Summary: {{ metadata.candidate }} vs {{ metadata.baseline }}

> **Key Finding**: `{{ metadata.candidate }}` achieved **{{ frequentist_stats.quality_pass_rate.candidate_mean }}% pass rate** (vs {{ frequentist_stats.quality_pass_rate.baseline_mean }}% baseline) with a **{{ frequentist_stats.quality_pass_rate.effect_magnitude }}** ($p = {{ frequentist_stats.quality_pass_rate.p_value }}$, $d = {{ frequentist_stats.quality_pass_rate.cohens_d }}$).

[![BenchForge Certified](https://img.shields.io/badge/BenchForge-Level_4_Adversarial_Audited-green.svg)](#)
[![Integrity Score](https://img.shields.io/badge/Integrity_Score-{{ integrity_score.overall_integrity_score }}%2F100-brightgreen.svg)](#)

---

### 📊 Performance at a Glance

| Metric | Baseline ({{ metadata.baseline }}) | **Candidate ({{ metadata.candidate }})** | Win Margin |
| :--- | :--- | :--- | :--- |
| **Test Pass Rate** | {{ frequentist_stats.quality_pass_rate.baseline_mean }}% | **{{ frequentist_stats.quality_pass_rate.candidate_mean }}%** | **{{ frequentist_stats.quality_pass_rate.effect_magnitude }}** |
| **Avg Wall Time** | {{ frequentist_stats.wall_time_ms.baseline_mean }} ms | **{{ frequentist_stats.wall_time_ms.candidate_mean }} ms** | **{{ frequentist_stats.wall_time_ms.effect_magnitude }}** |
| **Composite Index** | {{ composite_index.baseline }} / 100 | **{{ composite_index.candidate }} / 100** | **P(A>B): {{ composite_index.bayesian_p_superiority }}** |

```mermaid
gantt
    title Execution Speed Comparison (Lower is Better)
    dateFormat X
    axisFormat %s sec
    section Baseline ({{ metadata.baseline }})
    {{ frequentist_stats.wall_time_ms.baseline_mean / 1000 }}s : 0, {{ frequentist_stats.wall_time_ms.baseline_mean / 100 }}
    section Candidate ({{ metadata.candidate }})
    {{ frequentist_stats.wall_time_ms.candidate_mean / 1000 }}s : 0, {{ frequentist_stats.wall_time_ms.candidate_mean / 100 }}
```

---

### 🧪 Key Test Case Highlights
1. **Luau DataStore Module Test**: Candidate generated 100% compilation-ready retry logic with zero `TODO` comments.
2. **Memory Leak Debugging Test**: Candidate identified root-cause unhooked signal connections in 1.4s.
3. **Architecture Contract Test**: Candidate separated client/server boundaries strictly adhering to DRY principles.

👉 **[Read Full Exhaustive Scientific Report (BENCHMARK.md)](file:///./BENCHMARK.md)** for complete statistical proofs, DAG Evidence Graph, and full testcase data points.
